import localFont from 'next/font/local'

const berkeleyMono = localFont({
  src: '../fonts/BerkeleyMono-Regular.woff2',
  display: 'swap',
})

const universLT = localFont({
  src: '../fonts/UniversLT.woff2',
  display: 'swap',
})

export { berkeleyMono, universLT }